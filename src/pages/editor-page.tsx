import { DynamicIsland } from "../components/dynamic-island";

const EditorPage = () => {
  return (
    <div className="page editor-page">
      <header className="page-header bg-white w-full h-14">
        <DynamicIsland />
      </header>
    </div>
  );
};

export default EditorPage;
